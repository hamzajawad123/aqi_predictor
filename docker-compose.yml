version: "3.9"

services:
  aqi:
    image: hamzajawad/lahore-aqi-predictor:latest
    build:
      context: .
      dockerfile: Dockerfile
    container_name: aqi_predictor
    env_file: .env
    ports:
      - "8000:8000"
      - "8501:8501"
    restart: unless-stopped
